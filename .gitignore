node_modules
dist
.vite

# esbuild output of `npm run build:api` (src/api/*.ts -> api/*.mjs).
# Regenerated by Vercel on every deploy (the `build` script runs `build:api`),
# so these bundles are build artifacts, not source. Run `npm run build:api`
# locally before `vercel dev`.
api/*.mjs
.DS_Store
*.log
.env
.env.local
coverage
.claude/
.worktrees

# Smoke-test / Playwright MCP artifacts
.playwright-mcp/
*.png
!public/icons/**/*.png
!public/*.png
test-results.txt
test_output.txt

.vercel
public/data/market-cache.json
.env*

# Local chistes backups dumped by scripts/seed-chistes.ts before a wipe
chistes-backup-*.txt

# Locally-saved wiki reference pages
Dictionary of Icons - Gamer Escape's Final Fantasy XIV (FFXIV, FF14) wiki.html
Dictionary of Icons - Gamer Escape's Final Fantasy XIV (FFXIV, FF14) wiki_files/
